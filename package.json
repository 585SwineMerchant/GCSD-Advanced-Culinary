{
  "name": "gcsd-advanced-culinary",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "node --test tests/*.test.mjs",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply gcsd-advanced-culinary --local",
    "db:migrate:remote": "wrangler d1 migrations apply gcsd-advanced-culinary --remote"
  },
  "devDependencies": {
    "wrangler": "^4.28.0"
  }
}
